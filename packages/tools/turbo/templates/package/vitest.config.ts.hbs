{{#if react}}
import reactConfig from '@starter/vitest/react'
{{else}}
import libraryConfig from '@starter/vitest/library'
{{/if}}
import { defineConfig, mergeConfig } from 'vitest/config'

export default mergeConfig(
  {{#if react}}
  reactConfig,
  {{else}}
  libraryConfig,
  {{/if}}
  defineConfig({
    resolve: {
      tsconfigPaths: true,
    },
    test: {
      exclude: ['node_modules'],
    },
  }),
)
