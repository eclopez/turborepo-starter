import * as React from 'react'
import clsx from 'clsx'

const {{ pascalCase name }} = (props: React.HTMLAttributes<HTMLDivElement>) => {
  const { className, ...other } = props;

  return (
    <div className={clsx('starter-{{dashCase name}}', className)} {...other}>
      {{ pascalCase name }} component
    </div>
  );
}

export { {{ pascalCase name }} }
