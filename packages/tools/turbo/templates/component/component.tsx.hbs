import * as React from 'react'
{{#if isUiComponent}}
import { cn } from '@/utils'
{{else}}
import { cn } from '@starter/ui'
{{/if}}

type {{ pascalCase name }}Props = React.ComponentPropsWithRef<'div'>

const {{ pascalCase name }} = (props: {{ pascalCase name }}Props) => {
  const { className, ...other } = props

  return (
    {{#if isUiComponent}}
    <div className={cn('ui-{{dashCase name}}', className)} {...other}>
    {{else}}
    <div className={cn('starter-{{dashCase name}}', className)} {...other}>
    {{/if}}
      {{ pascalCase name }} component
    </div>
  )
}

export { {{ pascalCase name }} }
export type { {{ pascalCase name }}Props }
