import * as React from 'react'
{{#if isUiComponent}}
import { twMerge } from '@/utils'
{{else}}
import { twMerge } from '@starter/ui'
{{/if}}

type {{ pascalCase name }}Props = React.ComponentPropsWithRef<'div'>

const {{ pascalCase name }} = (props: {{ pascalCase name }}Props) => {
  const { className, ...other } = props

  return (
    {{#if isUiComponent}}
    <div className={twMerge('ui-{{dashCase name}}', className)} {...other}>
    {{else}}
    <div className={twMerge('starter-{{dashCase name}}', className)} {...other}>
    {{/if}}
      {{ pascalCase name }} component
    </div>
  )
}

export { {{ pascalCase name }} }
export type { {{ pascalCase name }}Props }
